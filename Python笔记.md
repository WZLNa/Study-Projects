# Python学习笔记

Updated:2025/12/8

## 一、Python基础

### Print--打印(输出)

```python
print(2024)  # 打印出2024
```

输出为：`2024`。

```python
print("hello world")  # 打印字符串
```

输出为：hello world

Python中绝大多数字符串的场景都需要加上双引号`" "`或者单引号`' '`

#### Print的一些进阶用法

**end**：设置打印结束后的字符，默认是换行符（`\n`）

```python
print("自行车",end="*")
print("阿萨德")
```

输出结果为：`自行车*阿萨德`

**sep**：设置打印多个内容的分隔符

```python
print(2025,"年，我要减肥",sep="*")
```

输出结果为：`2025*年，我要减肥`

#### Print格式化输出

```python
print("%s,你好" % ("小明"))  # 输出为:小明，你好
```

%s的意思：创建一个字符串占位符，用于接收后面的字符串

**一些其他的占位符：**

`%d`:整数占位符

`%02d`:整数占位符，不足2位用0填充

`%s`:字符串占位符

`%.1f`:浮点数占位符，保留1位小数



Tips:字符串占位符也可以接受数字，只不过输出的数字是字符串类型的

eg:

```python
mouth=8  # 创建了一个名为"mouth"的变量，它的值为8
```







### Python注释

Python注释分为：块注释、行内注释、多行注释

```python
print(111)  # 我是行内注释

'''
我是多行注释
我是多行注释2
'''

# 我是块注释
```

注释里的内容主要供开发者查看，起到非常大的作用，一个代码结构清晰、注释完整的代码后期调试起来更简便、更轻松。

注释里的代码不会被执行。

